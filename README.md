# My PAT Loader

This project is designed to load a .PAT (AutoCAD Hatch Pattern) file and generate a sequence of lines based on a given rendering scope.

## Features

- Parses .PAT file format.
- Generates line segments based on pattern definitions.
- Allows specifying a rendering area to determine the extent of generated lines. 